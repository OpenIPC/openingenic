MODULE_NAME := sensor_ps5520_t41
OUT := $(MODULE_NAME)

DIR=sensors/$(SOC)/ps5520

SRCS := $(DIR)/ps5520/ps5520.c

OBJS := $(SRCS:%.c=%.o) $(ASM_SRCS:%.S=%.o)

$(OUT)-objs := $(OBJS)

obj-m += $(OUT).o

